# Shared branch-protection defaults — referenced via YAML merge key below.
branch_protection_defaults: &branch_protection_defaults
  required_status_checks: []
  require_up_to_date: false
  bypass_pull_request: false
  bypass_actors_users: []
  bypass_actors_teams: []
  bypass_actors_apps:
    - blinklabs-governance
  required_approving_review_count: 1
  dismiss_stale_reviews: true
  require_code_owner_reviews: false
  enforce_admins: false
  require_linear_history: false
  required_conversation_resolution: false
  allow_force_pushes: false
  allow_deletions: false
  lock_branch: false

repositories:
  - name: blinklabs-io/tx-submit-api
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        # required_status_checks will be updated after the first PR run
        # confirms the generated workflow check names (orchestrate / <job>).
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: go-test.yml
        workflow_name: "go-test"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-go-test.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']

      - destination_file: golangci-lint.yml
        workflow_name: "golangci-lint"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-golangci-lint.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']

      - destination_file: nilaway.yml
        workflow_name: "nilaway"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-nilaway.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']
        params:
          include-pkgs: "github.com/blinklabs-io/tx-submit-api"

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - cmd/**
              - docs/**
              - internal/**
              - go.*
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/tx-submit-api"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          actions: write
          attestations: write
          checks: write
          contents: write
          id-token: write
          packages: write
          statuses: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          binary-name: "tx-submit-api"
          docker-image: "blinklabs/tx-submit-api"
          description: "A Go implementation of the Cardano Submit API service"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/tx-submit-api-mirror
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: golangci-lint.yml
        workflow_name: "golangci-lint"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-golangci-lint.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']

      - destination_file: nilaway.yml
        workflow_name: "nilaway"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-nilaway.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']
        params:
          include-pkgs: "github.com/blinklabs-io/tx-submit-api-mirror"

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - cmd/**
              - docs/**
              - internal/**
              - go.*
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/tx-submit-api-mirror"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          actions: write
          attestations: write
          checks: write
          contents: write
          id-token: write
          packages: write
          statuses: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          binary-name: "tx-submit-api-mirror"
          docker-image: "blinklabs/tx-submit-api-mirror"
          description: "A Cardano Submit API mirror service"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/txtop
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: go-test.yml
        workflow_name: "go-test"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-go-test.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']

      - destination_file: golangci-lint.yml
        workflow_name: "golangci-lint"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-golangci-lint.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']
        params:
          go-version: "1.26.x"

      - destination_file: nilaway.yml
        workflow_name: "nilaway"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-nilaway.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']
        params:
          include-pkgs: "github.com/blinklabs-io/txtop"
          go-version: "1.26.x"

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - '*.go'
              - go.*
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/txtop"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          actions: write
          attestations: write
          checks: write
          contents: write
          id-token: write
          packages: write
          statuses: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          binary-name: "txtop"
          docker-image: "blinklabs/txtop"
          description: "txtop is a mempool display tool for a Cardano Node"
          go-version: "1.26.x"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/docker-go
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/go"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish-docker.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          contents: write
          packages: write
          id-token: write
          attestations: write
          security-events: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          docker-image: "blinklabs/go"
          ghcr-image: "blinklabs-io/go"
          description: "Go image built from Chainguard Wolfi"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/cardano-node-api
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: go-test.yml
        workflow_name: "go-test"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-go-test.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']

      - destination_file: golangci-lint.yml
        workflow_name: "golangci-lint"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-golangci-lint.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']

      - destination_file: nilaway.yml
        workflow_name: "nilaway"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-nilaway.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']
        params:
          include-pkgs: "github.com/blinklabs-io/cardano-node-api"

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - Makefile
              - cmd/**
              - docs/**
              - internal/**
              - openapi/**
              - go.*
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/cardano-node-api"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          actions: write
          attestations: write
          checks: write
          contents: write
          id-token: write
          packages: write
          statuses: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          binary-name: "cardano-node-api"
          docker-image: "blinklabs/cardano-node-api"
          description: "HTTP API over the Cardano Node UNIX socket"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/shai
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: go-test.yml
        workflow_name: "go-test"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-go-test.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']
        params:
          go-versions: '["1.26.x"]'

      - destination_file: golangci-lint.yml
        workflow_name: "golangci-lint"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-golangci-lint.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']
        params:
          go-version: "1.26.x"

      - destination_file: nilaway.yml
        workflow_name: "nilaway"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-nilaway.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']
        params:
          include-pkgs: "github.com/blinklabs-io/shai"
          go-version: "1.26.x"

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - '*.go'
              - go.*
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/shai"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          actions: write
          attestations: write
          checks: write
          contents: write
          id-token: write
          packages: write
          statuses: write
        params:
          binary-name: "shai"
          go-version: "1.26.x"
          binary-os-matrix: '["linux"]'

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/docker-cardano-configs
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - config/**
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/cardano-configs"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish-docker.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          contents: write
          packages: write
          id-token: write
          attestations: write
          security-events: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          docker-image: "blinklabs/cardano-configs"
          ghcr-image: "blinklabs-io/cardano-configs"
          description: "Configuration files for named Cardano blockchain networks"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/docker-minio
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/minio"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish-docker.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          contents: write
          packages: write
          id-token: write
          attestations: write
          security-events: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          docker-image: "blinklabs/minio"
          ghcr-image: "blinklabs-io/minio"
          description: "Wrapper image for https://hub.docker.com/r/minio/minio"
          enable-attestation: "true"
          build-args: |
            VERSION=${{ github.ref_type == 'tag' && github.ref_name || '' }}
            COMMIT_HASH=${{ github.sha }}

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/docker-openvpn
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/openvpn"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish-docker.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
          schedule:
            - cron: '0 0 * * 1'
        permissions:
          contents: write
          packages: write
          id-token: write
          attestations: write
          security-events: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          docker-image: "blinklabs/openvpn"
          ghcr-image: "blinklabs-io/openvpn"
          description: "Simple OpenVPN image"
          enable-trivy-scan: "true"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/docker-cardano-cli
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker-multiarch.yml@main"
        triggers:
          pull_request:
            branches: [main, 'release/**']
            paths:
              - Dockerfile
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/cardano-cli"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish-docker-multiarch.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          contents: write
          packages: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          docker-image: "blinklabs/cardano-cli"
          ghcr-image: "blinklabs-io/cardano-cli"
          description: "Cardano CLI built from source on Debian"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/docker-cardano-node
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker-multiarch.yml@main"
        triggers:
          pull_request:
            branches: [main, 'release/**']
            paths:
              - Dockerfile
              - 'bin/**'
              - 'config/**'
              - .github/workflows/ci-docker.yml
        matrix:
          image: [cardano-node, cardano-tracer, cardano-submit-api]
        params:
          image-name: "blinklabs-io/${{ matrix.image }}"
          build-target: "${{ matrix.image }}"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish-docker-multiarch.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          contents: write
          packages: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          docker-image: "blinklabs/cardano-node"
          ghcr-image: "blinklabs-io/cardano-node"
          description: "Cardano Node built from source on Debian"
          build-target: "cardano-node"
          prerelease-pattern: "-pre-"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/docker-cardano-wallet
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker-multiarch.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - 'bin/**'
              - 'config/**'
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/cardano-wallet"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish-docker-multiarch.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          contents: write
          packages: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          docker-image: "blinklabs/cardano-wallet"
          ghcr-image: "blinklabs-io/cardano-wallet"
          description: "Cardano Wallet built from source on Debian"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        permissions:
          contents: read
          issues: read
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/docker-parity-subkey
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker-multiarch.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/subkey"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish-docker-multiarch.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          contents: write
          packages: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          docker-image: "blinklabs/subkey"
          ghcr-image: "blinklabs-io/subkey"
          description: "Polkadot SDK subkey binary built from source on Debian"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        permissions:
          contents: read
          issues: read
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/docker-wireguard
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - 'bin/**'
              - 'cmd/**'
              - 'internal/**'
              - go.*
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/docker-wireguard"

      - destination_file: go.yml
        workflow_name: "go-test"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-go-test.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']
        params:
          go-versions: '["1.25.x"]'
          test-flags: "-race"

      - destination_file: golangci-lint.yml
        workflow_name: "golangci-lint"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-golangci-lint.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']

      - destination_file: nilaway.yml
        workflow_name: "nilaway"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-nilaway.yml@main"
        triggers:
          pull_request:
          push:
            branches: [main]
            tags: ['v*']
        params:
          include-pkgs: "github.com/blinklabs-io/docker-wireguard"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          actions: write
          attestations: write
          checks: write
          contents: write
          id-token: write
          packages: write
          statuses: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          binary-name: "wg-peer-api"
          docker-image: "blinklabs/wireguard"
          description: "WireGuard VPN container with JWT-authenticated peer API"
          go-version: "1.25.x"
          binary-os-matrix: '["linux"]'
          binary-compress: "true"
          build-args: |
            VERSION=${{ github.ref_type == 'tag' && github.ref_name || '' }}
            COMMIT_HASH=${{ github.sha }}

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        permissions:
          contents: read
          issues: read
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"

  - name: blinklabs-io/docker-hydra-node
    settings:
      delete_branch_on_merge: true
    collaborators: []
    branch_protection:
      - branch: main
        <<: *branch_protection_defaults
    workflows:
      - destination_file: conventional-commits.yml
        workflow_name: "Conventional Commits"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-conventional-commits.yml@main"
        triggers:
          pull_request:

      - destination_file: ci-docker.yml
        workflow_name: "Docker CI"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-ci-docker-multiarch.yml@main"
        triggers:
          pull_request:
            branches: [main]
            paths:
              - Dockerfile
              - 'bin/**'
              - 'config/**'
              - .github/workflows/ci-docker.yml
        params:
          image-name: "blinklabs-io/hydra-node"

      - destination_file: publish.yml
        workflow_name: "publish"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-publish-docker-multiarch.yml@main"
        triggers:
          push:
            branches: [main]
            tags: ['v*.*.*']
        permissions:
          contents: write
          packages: write
        secrets:
          docker-password: DOCKER_PASSWORD
        params:
          docker-image: "blinklabs/hydra-node"
          ghcr-image: "blinklabs-io/hydra-node"
          description: "Hydra Node built from source on Debian"
          prerelease-pattern: "-pre-"

      - destination_file: test-issue-on-close.yml
        workflow_name: "Test Issue Close Trigger"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-test-issue-on-close.yml@main"
        triggers:
          issues:
            types: [closed]
        params:
          issue_number: "${{ github.event.issue.number }}"
          issue_title: "${{ github.event.issue.title }}"

      - destination_file: update-issue-on-close.yml
        workflow_name: "Set Project Closed Date"
        reusable_workflow: "blinklabs-io/actions/.github/workflows/reuseable-set-project-closed-date.yml@main"
        triggers:
          issues:
            types: [closed]
          workflow_dispatch:
        permissions:
          contents: read
          issues: read
        secrets:
          project_pat: ORG_PROJECT_PAT
        params:
          project_url: "https://github.com/orgs/blinklabs-io/projects/11"
          closed_date_field: "Closed Date"
          closed_at: "${{ github.event.issue.closed_at }}"
